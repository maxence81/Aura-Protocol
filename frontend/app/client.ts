import { createThirdwebClient } from "thirdweb";

// Replace with your actual client ID from Thirdweb dashboard if needed
export const client = createThirdwebClient({
  clientId: "aura-hackathon-id", 
});
